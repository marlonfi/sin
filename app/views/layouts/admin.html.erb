<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="">
  <meta name="author" content="Iokero">
  <meta name="keyword" content="Iokero, Sinesss">
  <title><%=full_title(yield(:title))%></title>
  <%= favicon_link_tag 'favicon.ico' %>
  <%= stylesheet_link_tag    "admin", media: "all", "data-turbolinks-track" => true %>
  <%= yield (:css_per_page)%>
  <%= csrf_meta_tags %>
  <!-- HTML5 shim and Respond.js IE8 support of HTML5 tooltipss and media queries -->
  <!--[if lt IE 9]>
    <script src="js/html5shiv.js"></script>
    <script src="js/respond.min.js"></script>
  <![endif]-->
</head>

<body>
  <section id="container" >
    <%= render 'layouts/admin_partials/header'%>
    <%= render 'layouts/admin_partials/aside'%>
    <section id="main-content">
      <section class="wrapper site-min-height">
        <%= render 'layouts/admin_partials/content_header'%>
        <%= yield %>
      </section>
    </section>    
    <%= render 'layouts/admin_partials/footer'%>
    <%= javascript_include_tag "admin", "data-turbolinks-track" => true %>
    <%= yield (:js_per_page)%>  
  </section>
</body>
</html>