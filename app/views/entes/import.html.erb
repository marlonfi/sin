<div class="col-lg-12">
    <section class="panel">
        <header class="panel-heading">
            Atención! Se van a importar entes. Previamente se debe haber importado las Redes Asitenciales. El archivo debe ser la MATRIZ proveida por ESSALUD en formato CSV. 
        </header>
        <div class="panel-body">
        		<%= form_tag entes_importar_path, multipart: true  do %>
	        		<div class="form-group">
	        			<label for="archivo">Archivo</label>
	        			<%= file_field_tag :archivo ,:accept => '.csv' %>
	        			<p class="help-block">El formato debe ser CSV.</p>
	        		</div>
	        		<div class="form-group">
	        			<label for="file">Descripción (MAX 250)</label>
	        			<%=text_area_tag 'descripcion', nil, rows: 5, class: 'form-control'%>
	        		</div>	        		
							<center><%= submit_tag "Procesar", class: 'btn btn-primary' %></center>
						<% end %> 
        </div>
    </section>
</div>